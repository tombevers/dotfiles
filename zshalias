# Aliases
alias tmux="TERM=screen-256color-bce tmux"
#alias composer='php /usr/bin/composer.phar'

# Include private aliases if file does exist
if [ -f ~/Dropbox/zshalias ]; then
    source ~/Dropbox/zshalias
fi
